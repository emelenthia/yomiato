# よみあと

## PR本文の書き方

`.github/pull_request_template.md` の節構成に従って書く。見出しと順序は変えず、`gh pr create` で本文を渡すときもテンプレートの節をそのまま使う。加えて次を守る。

- 完了した変更の説明なので「〜します」「〜しました」ではなく「〜した」で書く
- 背景には、変更前に利用者が何をできなかったか、何が不便だったかを書く。`docs/implementation-guide.md` の工程番号を書いただけで背景の代わりにしない
- 変更内容は、利用者から見て挙動がどう変わるかを書く。追加したファイル・関数・コンポーネントの列挙や、diffを読めば分かることの言い換えにしない
- 判断が必要だった箇所を書く。採用した仕様、却下した代替案、既存のDomain・Application層を再利用した理由、入力上限・確認文字列・並び順などの具体値をその値にした理由を含める
- 画面の変化は、どの画面のどこに何が表示され、利用者がどう操作するかを文章で書く。Chromeを起動できずスクリーンショットを撮れない環境では、この文章で代替する
- 確認したことには、コマンド名だけでなく結果まで書く（例: `npm test` — 105件成功）。skipしたテストは件数と理由を書く
- 実行していない確認を、確認済みとして書かない。自動テストで代替した確認はその旨を書き、Chromeへ拡張を読み込んでの手動確認をしていない場合も明記する
- lint自動修正、フォーマット、依存の再生成など、規約に従っただけの付随作業は変更内容に列挙しない
- MVPの境界（ページ本文・画像の取得、外部通信、認証、解析SDK、Content Script、host permissions、権限追加）に触れたかどうかを変更内容の最後に1行で書く

## Code Review Rules

### MVPの境界と利用者データ

- `docs/implementation-guide.md` のMVP固定仕様と `docs/product-plan.md` のP0要件に反する変更を指摘する。特に、ページ本文・画像・選択テキスト・閲覧履歴の取得、外部通信、認証、解析SDK、Content Script、host permissions、`<all_urls>` の追加は重大な問題として扱う。安全な実装は、必要最小限のChrome権限だけを使い、URL・タイトル・日時・利用者の記録を端末内だけに保存すること。

### データ整合性と状態遷移

- URL正規化後のInbox重複、同一ページの再読記録の喪失、読了・断念・削除の状態遷移の誤りを指摘する。IndexedDBの変更では、一意制約、トランザクション、マイグレーション、JSONインポート時の厳密な検証・参照整合性・失敗時のロールバックが守られていない場合を指摘する。安全な実装は、必要な複数レコードを同じトランザクションで変更し、インポート全体を検証してから反映すること。

### 拡張機能の実行環境と依存方向

- Manifest V3のService Workerが停止・再開される前提を欠く実装、またはDomainがReact・Dexie・Chrome APIへ依存する変更を指摘する。安全な実装は、永続化が必要な状態を保存し、UI・Application・InfrastructureからDomainへの依存方向を保つこと。

レビューは日本語で、利用者またはデータへの具体的な影響がある問題だけを指摘する。好み、根拠のない将来対応、MVP外機能の提案、整形・lintなどCIで判定できる事項は指摘しない。
