## 背景

<!-- 変更前に利用者が何をできなかったか、何が不便だったかを書く。diffを見れば分かることや、docs/implementation-guide.md の工程番号だけを書かない -->

## 変更内容

<!-- 利用者から見て挙動がどう変わるかを書く。追加したファイル・関数・コンポーネントの列挙にしない。判断が必要だった箇所（採用した仕様、却下した代替案、既存実装を再利用した理由、上限値や並び順をその値にした理由）を書く。最後に、MVPの境界（ページ本文・画像の取得、外部通信、権限追加）に触れたかどうかを1行で書く -->

## 画面

<!-- UIを変えた場合のみ。どの画面のどこに何が表示され、どう操作するかを書く。スクリーンショットを撮れる環境なら貼る。UI変更がなければこの節ごと削除する -->

## 確認したこと

<!-- 実行したコマンドと結果を書く。「npm test」ではなく「npm test — 105件成功」。skipしたテストは件数と理由を書く。Chromeへ読み込んでの手動確認をしていない場合はその旨を書く -->
